/**
 * Cloudinary 上の「公式カタログ用」3D サムネイル PNG の格納フォルダ（upload の folder と GET のパスで共通）。
 *
 * 【方針転換・260726 クライアント要望②】過去はキャッシュ無効化のたびにフォルダを版上げ（_v2/_v3）して増やしていたが、
 * これをやめ、フォルダは「1つ」に統一する。今後の無効化は、フォルダを増やすのではなく public_id 末尾の
 * キャッシュバスト・トークン（CLOUDINARY_THUMBNAIL_CACHE_BUST）を上げることで行う（＝ファイル名側で世代管理）。
 * これにより公式カタログ用サムネイルの保存先は常にこの1フォルダに集約され、旧 _v2/_v3 フォルダは不要（運営が削除）。
 *
 * 【重要・責務分担／260726 クライアント要望③】このフォルダは "公式（運営アップロード）" 専用。
 * 一般ユーザーがアップロードした 3D モデルのサムネイルは Cloudinary へ送らない（Supabase 側で扱う）。
 * サムネイル生成のアップロード経路は、対象が公式（Cloudinary 3d_assets）URL のときだけ Cloudinary に保存する。
 */
export const CLOUDINARY_THUMBNAIL_FOLDER = '3d_assets_thumbnails';

/**
 * サムネイルのキャッシュバスト・トークン（260726）。public_id/配信URLの拡張子直前に `__<token>` として付与する。
 * サムネイル生成ロジックを変えて再生成させたいときは、フォルダを増やさずこの値だけを上げる（例: '260726' → '260727'）。
 * 空文字にすると付与しない（後方互換）。旧 _v2/_v3 フォルダの残存物と public_id が衝突しないよう、移行時は非空の値にする。
 */
export const CLOUDINARY_THUMBNAIL_CACHE_BUST = '260730';

/**
 * サムネイルの「作り方」の版（260730）。生成ロジック（寄り・解像度・向き等）を変えたら上げる。
 *
 * Cloudinary 側（公式カタログ）は上の CLOUDINARY_THUMBNAIL_CACHE_BUST で無効化できるが、
 * ユーザーがアップロードしたモデルのサムネイルは Supabase にあり、URL が台帳(metadata)に
 * 焼き付いているため、そちらは別に無効化しないと古い見た目のまま残る。
 * 公式だけ作り直されてユーザー分が旧のままだと、一覧で寄りがバラバラになる。
 * 保存時にこの版を metadata へ書き、読み出し時に版が違えば「サムネイル無し」として扱って再生成させる。
 */
export const THUMBNAIL_FRAMING_VERSION = '260730';
