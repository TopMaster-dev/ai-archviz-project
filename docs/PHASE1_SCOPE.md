# Arise フェーズ1（MVP）合意済み仕様 — 実装の正

クライアント（合同会社SynergyPalette）と合意したフェーズ1の実装範囲。
凡例: ✅ 実装済 / 🔨 フェーズ1で実装 / ⏭ フェーズ2へ切り分け

## 0. 取引条件（前提）
- 金額: 基本 42万円 ＋ 追加 Plan A 15万円 ＝ 約57万円（15万は別途ココナラ追加見積もりで決済）。
- 納期: クライアント認識は 6/25（全スコープにはタイト。ステージング先行共有=「まず使える版」で運用）。着手後に初期監査→確定。
- 著作権: 納品・検収完了時に全面移転（著作権法27・28条含む、カーブアウトなし）。補助金エビデンスとして書面明記。
- インフラ: Vercel ＋ Supabase（認証/DB）＋ Cloudinary（素材・軽量化継続）。常時SSL・定期バックアップ。
- コミュニケーション: 数日に1回の進捗共有＋ステージングURL随時共有。
- コード品質: 標準的で可読・保守性の高いコード＋複雑処理にコメント＋引き継ぎ設計メモ（必須要件）。

## 1. 認証・アカウント（属性別登録）
- 🔨 メール＋パスワード認証、ログイン/ログアウト、パスワード変更・再設定、マイページ、利用規約・プライバシーポリシー
- 🔨 属性別登録: プロ／学生／施主。学生は「卒業予定年度」必須（DBで CHECK 済 ✅）
- 🔨 学生ポートフォリオのステルス蓄積（別ストア、3ヶ月削除対象外）— テーブル設計✅、蓄積処理🔨
- ⏭ 電話番号認証・SNSログイン

## 2. 課金・制限
- 🔨 BYOK: 各テストユーザーが自身のGeminiキーを画面で入力・保存→自分のキーで生成（利用上限なし・利用期間のみ制限）。「50枚無料トークン」記載は保留。
- 🔨 トークン計測「基盤」のみ用意（テスト期間は無効/無制限、後から有効化可能）
- 🔨 フリープラン保存上限（暫定5件、トリガ✅）
- ✅ 3ヶ月自動削除バッチ（登録3ヶ月超のフリーデータを 論理削除→猶予→物理削除、pg_cron）。Cloudinary資産削除はアプリ層で別途🔨
- ⏭ 有料プラン・従量課金・決済連携・プラン変更

## 3. プロジェクト保存・共有
- 🔨 per-user 永続化（projects.data jsonb）＋ autosave。2Dスケッチ/3D/マテリアル割当/AI編集履歴/カメラ視点を保存（スキーマ✅）
- 🔨 プロジェクト作成・複製
- 🔨 共有: 閲覧用URL発行（view only）＋ 複製（トークン方式、スキーマ✅）
- ⏭ リアルタイム同期・きめ細かな権限管理

## 4. 2Dスケッチ（平面図）
- 🔨（既存最適化）壁/頂点/開口部（窓・ドア）、グリッド/長さ/角度スナップ
- 🔨 下絵挿入（PDF/JPEG/PNG 背景）＋ 容量制限
- 🔨 ショートカット: Ctrl+Z/Ctrl+Y（Undo/Redo）、Ctrl+G（グループ化）— 主要操作対象、状態管理リファクタ前提
- 🔨 幾何的な寸法スナップ
- 🔨 天井オブジェクト配置（照明・梁など）
- 🔨 梁（サイズ可変）= パラメトリック（窓・ドア同様に2Dで配置・寸法調整）
- ⏭ CADデータからのスナップ、⏭ 柱（梁と同系統で Phase 2）

## 5. 3Dビュー
- 🔨（既存最適化）2D→3D自動立ち上げ、天井高mm、窓背景（昼/夕/夜）、カタログ素材貼付、素材調整、家具配置・2D/3D連動、サムネ自動生成、カメラ操作
- 🔨 天伏ビュー（実用最小ライン）: 平面を半透明表示し天井レイヤを設定。高度レイヤ編集は⏭
- 🔨 実寸テクスチャ自動投影（平面サーフェスのみ）: 壁/床/天井に実寸リピート投影。素材実寸メタ層✅、RoomViewer 結線🔨
- 🔨 テクスチャの向き変更、スケルトン天井、ローカル画像を素材追加
- 🔨 天井オブジェクト配置（クライアント支給GLB: ≤2MB/点・~1〜3万ポリゴン・テクスチャ≤2048px・Draco圧縮GLB）
- ⏭ 曲面壁/間仕切り壁、プッシュプル、品番自動差替、ユーザー3D取込、タイル割付パターン(K01/K02)、外部3D投影

## 6. AIレンダリング・AI画像編集（既存最適化）
- 🔨（既存最適化）3D→フォトリアルAI変換（Gemini）、マルチ参照編集、配置ナラティブ
- 🔨 AI生成精度の初期チューニング（実用水準まで／無制限の追求は⏭）:
  - *1 カメラ挙動の精査・最適化
  - *2 AI編集UX・マスク生成精度の調整
  - *3 繰り返し編集の品質維持（ControlNet/Denoising系）
  - *4 印刷向け高解像度化（Real-ESRGAN系）

## 7. AIマジックツール UI（add-on / Plan A）
※クライアントUIイメージPDFが「正」
- 🔨 3モード配置: エリア編集 / コーディネート / エージェントに相談、選択中は緑ハイライト
- 🔨 エリア編集: 範囲指定して修正指示（範囲追加・画像選択）
- 🔨 コーディネート: プロンプト未入力でも実行可（完全お任せ）／実行ボタン「AIでコーディネートを実行」／任意でプロンプト＋ファイル添付
- 🔨 エージェントに相談: チャット形式の相談パネル（例示プレースホルダ）— Phase1は基本チャット。パーソナライズ学習エージェントは⏭

## 8. AI学習基盤（add-on / Plan A）
- 🔨 good/bad 評価ボタン（生成画像付近、hoverで「良い/悪い回答」＋アイコンハイライト）
- ✅ フィードバックログDB（分離済・3ヶ月削除対象外、migration 0002）
- ✅ 夜間定期集計（要約）バッチ（pg_cron、別系統）
- 🔨 in-context 活用: 集計した良い知見を生成プロンプトへ差し込み（個別ユーザー＋簡易な全ユーザー還元）
- 🔨 暗黙的フィードバック（高価値な少数のみ）: ①戻して同一プロンプト再生成＝bad ②書き出し/保存/見積採用/サムネ設定＝good（重み付き集計）
- 🔨 初回利用時の評価促進ポップアップ
- ⏭ 全ユーザー強化学習・モデル個別化/ファインチューニング・高度な暗黙評価モデル

## 9. 見積パネル・出力（既存最適化）
- 🔨（既存最適化）家具/建材見積、AI編集由来項目の反映、PDF/CSV書き出し
- 🔨 *5 マテリアルボード書き出し（A3・実寸100mm角・物件名/会社名のレイアウト）

## 10. 全体最適化・リファクタリング（必須要件・横断）
- 🔨 非エンジニアCursorプロトタイプ → 複数人が安全に実運用できる土台へリファクタリング/一部再設計
- 🔨 状態管理の再設計（Zustand 統合ストア＋Undo/Redo＋グループ化）
- 🔨 デッドコード整理（denoise未接続 / 独自GLB読込ボタン未接続 / pathtracer未使用 / services/* 空）
- 🔨 可読性・保守性の確保＋コメント＋引き継ぎ設計メモ

## フェーズ2への明示的な切り分け（境界）
CADスナップ／リアルタイム同期・高度権限／天伏の高度レイヤ編集／曲面・間仕切り壁・プッシュプル・品番自動差替・ユーザー3D取込・タイル割付・外部3D投影／柱／有料課金・決済／全ユーザー強化学習・ファインチューニング・パーソナライズドエージェント／多言語・サポート基盤・メーカー発注連携 など（補助金概算 約90〜170万円の範囲）。

---
*このドキュメントは合意の記録（spec of record）。変更はクライアント合意のうえ追記する。*
